# Setup Instructions

## Installation
```bash
pip install -r requirements.txt
```

## Run the App
```bash
streamlit run app.py
```

## Notes
- Keep dataset in `dataset/` folder.
- Trained models will be saved in `models/`.
